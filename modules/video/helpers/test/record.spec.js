'use strict';
const expect = require('chai').expect;
var record = require('../../helpers/record');
var path = require('path');
var fs = require('fs');

describe('Video', () => {
	describe('helpers', () => {
		describe('record', () => {});
	});
});
